# AGENTS.md

> Cross-platform agent instructions for opsward.
> Generated by [opsward](https://pypi.org/project/opsward/).

## Project Overview

Diagnose, generate, and maintain the AI agent setup of your projects — CLAUDE.md, skills, subagents, rules, and supporting docs.

**Tech stack:** Python 3.10+, `argh` (CLI), `string.Template` (templates), `dataclasses`. Lightweight by design — no heavy deps.

## Build & Test Commands

```bash
pip install -e ".[dev]"
pytest                              # unit + integration tests
pytest --doctest-modules opsward/   # doctests embedded in the package
```

## Project Structure

- `opsward/` — the package (facade in `__init__.py`)
  - `scan.py` — read-only filesystem scanning (hard invariant: never writes)
  - `score.py` — quality scoring; rubric is hardcoded constants (`_OVERALL_WEIGHTS`, `_*_MAX`)
  - `generate.py` — template rendering + file generation (never overwrites; dry-run by default)
  - `maintain.py` — staleness / drift detection
  - `recommend.py` — tech-stack → curated ecosystem-skill recommendations
  - `cli.py` / `__main__.py` — `argh` dispatch
  - `base.py` — dataclasses (`ScanResult`, `DiagnosisReport`, `ComponentScore`, …)
  - `data/templates/` — bundled templates (`shared/`, `python/`, `jsts/`), accessed via `importlib.resources`
- `tests/` — pytest tests + sample-project fixtures under `tests/fixtures/`
- `misc/docs/` — project documentation (entry point: `docs_guide.md`)
- `pyproject.toml` — packaging, deps, tool config

## Code Style

- Use `ruff format` for formatting
- Use `ruff` for linting

## Testing

- **Framework:** pytest
- **Test directory:** tests
- **Run tests:** `pytest`
- **Coverage:** `pytest --cov`

## Git Workflow

- Use conventional commits: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`
- Create feature branches; submit pull requests for review.
- Do not push directly to main/master.

## Boundaries

- Do not modify generated files or lock files manually.
- Do not commit secrets, credentials, or `.env` files.
- Ask before making destructive changes (deleting files, dropping tables, force-pushing).
