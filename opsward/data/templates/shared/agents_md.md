# AGENTS.md

> Cross-platform agent instructions for ${project_name}.
> Generated by [opsward](https://pypi.org/project/opsward/).

## Project Overview

${project_description}

**Tech stack:** ${tech_stack}

## Build & Test Commands

${commands}

## Project Structure

${module_map}

## Code Style

${conventions}

## Testing

- **Framework:** ${test_framework}
- **Test directory:** ${test_dir}
- **Run tests:** `${test_command}`
- **Coverage:** `${coverage_command}`

## Git Workflow

- Use conventional commits: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`
- Create feature branches; submit pull requests for review.
- Do not push directly to main/master.

## Boundaries

- Do not modify generated files or lock files manually.
- Do not commit secrets, credentials, or `.env` files.
- Ask before making destructive changes (deleting files, dropping tables, force-pushing).
